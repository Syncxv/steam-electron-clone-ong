import Layout from "../components/Layout";

const IndexPage = () => {
    return (
        <Layout title="Home | Next.js + TypeScript + Electron Example">
            <h1 className="text-xl font-bold text-green-500">hi</h1>
        </Layout>
    );
};

export default IndexPage;
